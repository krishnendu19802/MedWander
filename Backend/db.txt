CREATE TABLE SUBMISSIONS (
    FORM_TYPE VARCHAR(1),
    NAME VARCHAR(255),
    COUNTRY_CODE VARCHAR(20),
    PHONE_NUMBER VARCHAR(10),
    TSTAMP TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    PRIMARY KEY(FORM_TYPE, COUNTRY_CODE, PHONE_NUMBER)
);
